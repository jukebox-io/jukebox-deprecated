import 'package:flutter/material.dart';

class MoreItemsScreen extends StatelessWidget {
  const MoreItemsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('More Screens'),
    );
  }
}
